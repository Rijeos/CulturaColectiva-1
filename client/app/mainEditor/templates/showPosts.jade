navbar
.container
  p
    | Lista de Posts
  ul.list-group
    li.list-group-item(ng-repeat='post in posts' )
      ul(style="list-style:none;")
        li(ng-repeat='version in post.VersionPosts')
         {{version.titulo}}         
      a.trash(ng-click='delete(post)')
        span.glyphicon.glyphicon-trash.pull-right